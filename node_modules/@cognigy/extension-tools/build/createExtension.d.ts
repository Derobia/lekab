import { INodeDescriptor } from "./interfaces/descriptor";
import { IConnectionSchema } from "./interfaces/connection";
import { IExtensionOptions } from "./interfaces/options";
interface ICreateExtensionParams {
    nodes: INodeDescriptor[];
    connections?: IConnectionSchema[];
    options?: IExtensionOptions;
}
export declare const createExtension: (params: ICreateExtensionParams) => {
    nodes: INodeDescriptor[];
    connections: IConnectionSchema[];
    options: IExtensionOptions;
};
export {};
