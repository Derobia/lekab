fatal.requestHandler = function (request, response) {
    console.log(request.type);
    REDISCLIENT.lrange("fatal",0,100, function (err, info) {
        REDISCLIENT.del("fatal", function (err, info) {
            response.end(JSON.stringify(result));
        })
    })
};

